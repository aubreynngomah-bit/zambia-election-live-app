services:
  - type: web
    name: zambia-election-live
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --config gunicorn.conf.py
    envVars:
      - key: FETCH_MINUTES
        value: "30"
    healthCheckPath: /health
